alpha = 30.0;
C = 10.0;
b = 5.0;
d = 1.0;
g = 1.0;

x=0;
y=10;
z=0;

mx=0;
my=0;
mz=0;

fz=[alpha/(1+(x/C)^2)];
fx=[alpha/(1+(y/C)^2)];
fy=[alpha/(1+(z/C)^2)];

->mx, [fx];
->my, [fy];
->mz, [fz];

mx->mx+x, b*d;
my->my+y, b*d;
mz->mz+z, b*d;

mx ->, d;
my ->, d;
mz ->, d;

x ->, g;
y ->, g;
z ->, g;


